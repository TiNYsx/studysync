## Installation
1. open new terminal in the base directory and run `cd client/`
2. run `npm install react-scripts`
3. run `npm run start`
4. open new terminal in the base directory and run `cd server/`
5. run `npm install express`
6. run `npm run start`
